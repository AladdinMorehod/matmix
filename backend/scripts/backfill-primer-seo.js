"use strict";

const fs = require("fs");
const path = require("path");
const sqlite3 = require("sqlite3").verbose();
const DATA = require("./data/primers-seo");
const CONFIRM = "BACKFILL_PRIMER_SEO";
const ALL_MATS = Object.freeze(DATA.TARGET_MATS);
const SEO_MUTABLE_FIELDS_EXACTLY = Object.freeze(["seo_title", "seo_description"]);
const IMMUTABLE_FIELDS = Object.freeze(["title", "slug", "brand", "weight", "price", "category", "subcategory", "short_description", "description", "full_description", "stock_status", "image", "image_url"]);
const normalizeStructure = value => String(value ?? "").normalize("NFKC").replace(/\s+/gu, " ").trim().toLocaleLowerCase("ru-RU");
const nonempty = value => value !== null && value !== undefined && String(value).trim() !== "";

function openDatabase(file, writable = false) {
  if (!file || file === ":memory:") throw new Error("Explicit --db path is required");
  const resolved = path.resolve(file); const mode = writable ? sqlite3.OPEN_READWRITE : sqlite3.OPEN_READONLY;
  return new Promise((resolve, reject) => { const raw = new sqlite3.Database(resolved, mode, error => { if (error) return reject(error); const db = { run(sql, params = []) { return new Promise((res, rej) => raw.run(sql, params, function done(e) { e ? rej(e) : res({ id: this.lastID, changes: this.changes }); })); }, get(sql, params = []) { return new Promise((res, rej) => raw.get(sql, params, (e, row) => e ? rej(e) : res(row))); }, all(sql, params = []) { return new Promise((res, rej) => raw.all(sql, params, (e, rows) => e ? rej(e) : res(rows))); }, close() { return new Promise((res, rej) => raw.close(e => e ? rej(e) : res())); } }; db.run("PRAGMA foreign_keys=ON").then(() => writable ? db : db.run("PRAGMA query_only=ON").then(() => db)).then(resolve).catch(async e => { await db.close(); reject(e); }); }); });
}
function parseArgs(args) { const out = { apply: false }; const seen = new Set(); for (let i = 0; i < args.length; i += 1) { const [key, ...tail] = args[i].split("="); if (seen.has(key)) throw new Error(`Duplicate option: ${key}`); seen.add(key); if (key === "--apply" || key === "--dry-run") { if (tail.length) throw new Error(`Unexpected value: ${key}`); out.apply = key === "--apply"; continue; } if (!["--db", "--only", "--confirm", "--backup-dir", "--review"].includes(key)) throw new Error(`Unknown option: ${key}`); const value = tail.length ? tail.join("=") : args[++i]; if (!value || value.startsWith("--")) throw new Error(`Value required: ${key}`); out[key.slice(2)] = value; } if (seen.has("--apply") && seen.has("--dry-run")) throw new Error("Choose either --dry-run or --apply"); if (!out.db || out.db === ":memory:") throw new Error("Explicit --db path is required"); if (out.only === undefined) throw new Error("Explicit --only is required"); out.only = out.only.split(",").map(v => v.trim()).filter(Boolean); if (!out.only.length) throw new Error("Nonempty --only is required"); if (seen.has("--confirm") && !out.apply) throw new Error("--confirm requires --apply"); if (out.apply && out.confirm !== CONFIRM) throw new Error(`Apply requires --confirm ${CONFIRM}`); return out; }
function snapshot(product) { return Object.fromEntries(IMMUTABLE_FIELDS.map(field => [field, product[field] ?? null])); }
function sameSnapshot(before, after) { return IMMUTABLE_FIELDS.every(field => (before[field] ?? null) === (after[field] ?? null)); }
function validation(config) { const issues = []; const title = String(config.proposedSeoTitle || ""); const description = String(config.proposedSeoDescription || ""); if (!title.trim() || !description.trim()) issues.push("empty SEO value"); if (title !== title.trim() || description !== description.trim()) issues.push("leading/trailing whitespace"); if (/\s{2,}/u.test(title) || /\s{2,}/u.test(description)) issues.push("duplicate spaces"); if (/[\r\n]/u.test(title) || /[\r\n]/u.test(description)) issues.push("embedded newline"); if (Array.from(title).length > 65) issues.push("SEO title exceeds 65 characters"); return [...new Set(issues)]; }
function guard(product, config) { if (!product || Number(product.is_active) !== 1 || product.deleted_at) throw new Error("Product missing, inactive, or deleted"); if (product.title !== config.expectedTitle) throw new Error(`Exact title guard failed: expected «${config.expectedTitle}», got «${product.title}»`); if (normalizeStructure(product.category) !== normalizeStructure(config.expectedCategory) || normalizeStructure(product.subcategory) !== normalizeStructure(config.expectedSubcategory)) throw new Error(`Category/subcategory guard failed: got ${product.category} / ${product.subcategory}`); }
async function inspectBatch(db, { only, data = DATA } = {}) { const selected = [...new Set(only || [])]; if (!selected.length) throw new Error("Explicit nonempty --only is required"); const rows = []; for (const externalId of selected) { const config = data.PRODUCTS.find(item => item.externalId === externalId); if (!config || !ALL_MATS.includes(externalId)) { rows.push({ externalId, status: "EXCLUDED_ALLOWLIST", reason: "MAT is outside exact primer SEO allowlist" }); continue; } try { const product = await db.get("SELECT * FROM products WHERE external_id=?", [externalId]); guard(product, config); const issues = validation(config); const same = product.seo_title === config.proposedSeoTitle && product.seo_description === config.proposedSeoDescription; const conflict = (nonempty(product.seo_title) && product.seo_title !== config.proposedSeoTitle) || (nonempty(product.seo_description) && product.seo_description !== config.proposedSeoDescription); const status = issues.length ? "ERROR" : same ? "EXISTING_OK" : conflict ? "CONTENT_CONFLICT" : "WILL_ADD"; rows.push({ externalId, productId: product.id, title: product.title, currentSeoTitle: product.seo_title || null, currentSeoDescription: product.seo_description || null, proposedSeoTitle: config.proposedSeoTitle, proposedSeoDescription: config.proposedSeoDescription, status, validationIssues: issues, sourceKeys: config.sourceKeys, notes: config.notes || [], immutableSnapshot: snapshot(product), seoTitleCharacterCount: Array.from(config.proposedSeoTitle).length, seoDescriptionCharacterCount: Array.from(config.proposedSeoDescription).length }); } catch (error) { rows.push({ externalId, title: config.expectedTitle, status: "ERROR", error: error.message, sourceKeys: config.sourceKeys }); } } const titles = rows.filter(r => r.proposedSeoTitle).map(r => r.proposedSeoTitle.toLocaleLowerCase("ru-RU")); const descriptions = rows.filter(r => r.proposedSeoDescription).map(r => r.proposedSeoDescription.toLocaleLowerCase("ru-RU")); const duplicateSeoTitles = titles.length - new Set(titles).size; const duplicateSeoDescriptions = descriptions.length - new Set(descriptions).size; if (duplicateSeoTitles || duplicateSeoDescriptions) for (const row of rows) if (row.status !== "ERROR") row.status = "ERROR"; return { mode: "dry-run", rows, sources: data.SOURCES, summary: { total: rows.length, willAdd: rows.filter(r => r.status === "WILL_ADD").length, existingOk: rows.filter(r => r.status === "EXISTING_OK").length, contentConflict: rows.filter(r => r.status === "CONTENT_CONFLICT").length, errors: rows.filter(r => r.status === "ERROR").length, excludedAllowlist: rows.filter(r => r.status === "EXCLUDED_ALLOWLIST").length, duplicateSeoTitles, duplicateSeoDescriptions } }; }
async function backupDatabase(dbPath, backupDir) { const dir = path.resolve(backupDir || path.join(path.dirname(dbPath), "backups")); fs.mkdirSync(dir, { recursive: true }); const target = path.join(dir, `matmix-before-primer-seo-${Date.now()}.db`); fs.copyFileSync(path.resolve(dbPath), target); return target; }
async function applyBatch(db, dbPath, options, data = DATA) { if (options.confirm !== CONFIRM) throw new Error(`Apply requires --confirm ${CONFIRM}`); const preflight = await inspectBatch(db, { only: options.only, data }); if (preflight.summary.errors || preflight.summary.excludedAllowlist || preflight.summary.contentConflict || preflight.summary.duplicateSeoTitles || preflight.summary.duplicateSeoDescriptions) throw new Error("SEO apply blocked by guard/conflict"); const targets = preflight.rows.filter(r => r.status === "WILL_ADD"); if (!targets.length) return { ...preflight, mode: "apply", writes: 0, backup: null }; const backup = await backupDatabase(dbPath, options.backupDir); await db.run("BEGIN IMMEDIATE"); let writes = 0; try { const now = new Date().toISOString(); for (const row of targets) { const result = await db.run("UPDATE products SET seo_title=?,seo_description=?,updated_at=? WHERE id=? AND external_id=? AND (seo_title IS NULL OR TRIM(seo_title)='') AND (seo_description IS NULL OR TRIM(seo_description)='')", [row.proposedSeoTitle, row.proposedSeoDescription, now, row.productId, row.externalId]); if (result.changes !== 1) throw new Error(`Write guard failed: ${row.externalId}`); writes += 1; } for (const row of targets) { const after = await db.get("SELECT * FROM products WHERE id=? AND external_id=?", [row.productId, row.externalId]); if (!after || after.seo_title !== row.proposedSeoTitle || after.seo_description !== row.proposedSeoDescription || !sameSnapshot(row.immutableSnapshot, after)) throw new Error(`Postcheck failed: ${row.externalId}`); } await db.run("COMMIT"); return { ...preflight, mode: "apply", writes, backup }; } catch (error) { await db.run("ROLLBACK").catch(() => {}); throw error; } }
function md(value) { return String(value ?? "—").replace(/\|/g, "\\|").replace(/\n/g, "<br>"); }
function renderReview(report) { const lines = ["# Грунтовки — SEO review", "", "Изменяются только `products.seo_title` и `products.seo_description`; production не используется.", "", `SEO_MUTABLE_FIELDS_EXACTLY = [\"seo_title\", \"seo_description\"]`, `CONFIRM = ${CONFIRM}`, "", "## Summary", "", "```json", JSON.stringify(report.summary, null, 2), "```", "", "| MAT | Status | Title chars | Description chars |", "|---|---|---:|---:|"]; for (const row of report.rows) lines.push(`| ${row.externalId} | ${row.status} | ${row.seoTitleCharacterCount || 0} | ${row.seoDescriptionCharacterCount || 0} |`); lines.push("", "## Proposed SEO", ""); for (const row of report.rows) lines.push(`### ${row.externalId}`, "", `- Title: ${md(row.title)}`, `- Status: **${row.status}**`, `- Source keys: ${md((row.sourceKeys || []).join(", "))}`, "", `**seo_title:** ${row.proposedSeoTitle || "—"}`, "", `**seo_description:** ${row.proposedSeoDescription || "—"}`, ""); lines.push("## Source registry", ""); for (const [key, source] of Object.entries(report.sources || {})) lines.push(`- **${key}**: ${source.url ? `[${source.title || key}](${source.url})` : source.title || key}`); return lines.join("\n") + "\n"; }
async function main() { const options = parseArgs(process.argv.slice(2)); const db = await openDatabase(options.db, options.apply); try { const report = options.apply ? await applyBatch(db, options.db, options) : await inspectBatch(db, options); if (options.review) { const base = path.resolve(options.review); fs.mkdirSync(path.dirname(base), { recursive: true }); fs.writeFileSync(`${base}.json`, JSON.stringify(report, null, 2) + "\n"); fs.writeFileSync(`${base}.md`, renderReview(report)); } for (const row of report.rows) console.log(JSON.stringify(row)); console.log(JSON.stringify({ mode: report.mode, summary: report.summary })); if (report.summary.errors || report.summary.contentConflict) process.exitCode = 1; } finally { await db.close(); } }
module.exports = { ALL_MATS, CONFIRM, DATA, SEO_MUTABLE_FIELDS_EXACTLY, IMMUTABLE_FIELDS, applyBatch, inspectBatch, normalizeStructure, openDatabase, parseArgs, renderReview, validation };
if (require.main === module) main().catch(error => { console.error(`PRIMER SEO BACKFILL ABORTED: ${error.message}`); process.exitCode = 1; });
