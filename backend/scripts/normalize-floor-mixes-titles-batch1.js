"use strict";

const fs = require("fs");
const path = require("path");
const sqlite3 = require("sqlite3").verbose();
const DATA = require("./data/floor-mixes-core-batch1");

const CONFIRM = "NORMALIZE_FLOOR_MIXES_TITLES_BATCH1";
const PRODUCTS = Object.freeze(DATA.PRODUCTS.filter(item => item.identityStatus === "READY_FOR_CORE_REVIEW"));
const ALL_MATS = Object.freeze(PRODUCTS.map(item => item.externalId));

function openDatabase(file, writable = false) {
  if (!file || file === ":memory:") throw new Error("Explicit --db path to an existing database is required");
  const resolved = path.resolve(file); const mode = writable ? sqlite3.OPEN_READWRITE : sqlite3.OPEN_READONLY;
  return new Promise((resolve, reject) => {
    const raw = new sqlite3.Database(resolved, mode, error => {
      if (error) return reject(error);
      const db = { run(sql, params = []) { return new Promise((res, rej) => raw.run(sql, params, function done(e) { e ? rej(e) : res({ id: this.lastID, changes: this.changes }); })); }, all(sql, params = []) { return new Promise((res, rej) => raw.all(sql, params, (e, rows) => e ? rej(e) : res(rows))); }, close() { return new Promise((res, rej) => raw.close(e => e ? rej(e) : res())); } };
      db.run("PRAGMA foreign_keys=ON").then(() => writable ? db : db.run("PRAGMA query_only=ON").then(() => db)).then(resolve).catch(async e => { await db.close(); reject(e); });
    });
  });
}

function assertExactBatch(values) { const normalized = values.map(value => String(value).trim()); if (normalized.length !== ALL_MATS.length || normalized.some((value, index) => value !== ALL_MATS[index])) throw new Error(`Exact title batch required: ${ALL_MATS.join(",")}`); return normalized; }
function parseArgs(args) { const out = { apply: false }; const seen = new Set(); for (let i = 0; i < args.length; i += 1) { const [key, ...tail] = args[i].split("="); if (seen.has(key)) throw new Error(`Duplicate option: ${key}`); seen.add(key); if (key === "--apply" || key === "--dry-run") { if (tail.length) throw new Error(`Unexpected value: ${key}`); out.apply = key === "--apply"; continue; } if (!["--db", "--only", "--confirm", "--backup-dir"].includes(key)) throw new Error(`Unknown option: ${key}`); const value = tail.length ? tail.join("=") : args[++i]; if (!value || value.startsWith("--")) throw new Error(`Value required: ${key}`); out[key === "--backup-dir" ? "backupDir" : key.slice(2)] = value; } if (seen.has("--apply") && seen.has("--dry-run")) throw new Error("Choose either --dry-run or --apply"); if (!out.db || !out.only) throw new Error("Explicit --db and --only are required"); out.only = assertExactBatch(out.only.split(",")); if (seen.has("--confirm") && !out.apply) throw new Error("--confirm requires --apply"); if (out.apply && out.confirm !== CONFIRM) throw new Error(`Apply requires --confirm ${CONFIRM}`); return out; }

async function inspectBatch(db, { only } = {}) { const selected = assertExactBatch(only || []); const rows = []; for (const externalId of selected) { const config = PRODUCTS.find(item => item.externalId === externalId); const products = await db.all("SELECT id,external_id,title FROM products WHERE external_id=?", [externalId]); if (products.length !== 1) { rows.push({ externalId, status: "ERROR", reason: `Expected one product, found ${products.length}.` }); continue; } const product = products[0]; if (product.title === config.titleCandidate) rows.push({ externalId, productId: product.id, currentTitle: product.title, targetTitle: config.titleCandidate, status: "EXISTING_OK" }); else if (product.title !== config.expectedTitle) rows.push({ externalId, productId: product.id, currentTitle: product.title, targetTitle: config.titleCandidate, status: "TITLE_GUARD_BLOCKED", reason: `Expected exact current title «${config.expectedTitle}», got «${product.title}».` }); else rows.push({ externalId, productId: product.id, currentTitle: product.title, targetTitle: config.titleCandidate, status: "WILL_FIX" }); } return { mode: "dry-run", rows, summary: { total: rows.length, willFix: rows.filter(row => row.status === "WILL_FIX").length, existingOk: rows.filter(row => row.status === "EXISTING_OK").length, titleGuardBlocked: rows.filter(row => row.status === "TITLE_GUARD_BLOCKED").length, errors: rows.filter(row => row.status === "ERROR").length } }; }
async function backupDatabase(dbPath, backupDir) { const dir = path.resolve(backupDir || path.join(path.dirname(dbPath), "backups")); fs.mkdirSync(dir, { recursive: true }); const target = path.join(dir, `matmix-before-floor-mixes-title-normalization-${new Date().toISOString().replace(/[:.]/g, "-")}.db`); fs.copyFileSync(path.resolve(dbPath), target); return target; }
async function applyBatch(db, dbPath, options) { const preflight = await inspectBatch(db, { only: options.only }); if (preflight.summary.errors || preflight.summary.titleGuardBlocked) throw new Error("Apply blocked by title guard"); const backup = await backupDatabase(dbPath, options.backupDir); await db.run("BEGIN IMMEDIATE"); let writes = 0; try { for (const row of preflight.rows.filter(item => item.status === "WILL_FIX")) { const result = await db.run("UPDATE products SET title=? WHERE id=? AND external_id=? AND title=?", [row.targetTitle, row.productId, row.externalId, row.currentTitle]); if (result.changes !== 1) throw new Error(`Title update guard failed: ${row.externalId}`); writes += 1; } const post = await inspectBatch(db, { only: options.only }); if (post.summary.errors || post.summary.titleGuardBlocked || post.summary.willFix) throw new Error("Title postcheck failed"); await db.run("COMMIT"); return { ...post, mode: "apply", writes, backup }; } catch (error) { try { await db.run("ROLLBACK"); } catch {} throw error; } }

module.exports = { ALL_MATS, CONFIRM, PRODUCTS, applyBatch, assertExactBatch, inspectBatch, openDatabase, parseArgs };
if (require.main === module) { const options = parseArgs(process.argv.slice(2)); openDatabase(options.db, options.apply).then(async db => { try { const report = options.apply ? await applyBatch(db, options.db, options) : await inspectBatch(db, options); report.rows.forEach(row => console.log(JSON.stringify(row))); console.log(JSON.stringify({ mode: report.mode, summary: report.summary, ...(report.backup ? { backup: report.backup, writes: report.writes } : {}) })); if (report.summary.errors || report.summary.titleGuardBlocked) process.exitCode = 1; } finally { await db.close(); } }).catch(error => { console.error(`FLOOR MIX TITLE NORMALIZATION ABORTED: ${error.message}`); process.exitCode = 1; }); }
