const path = require("path");
const sqlite3 = require("sqlite3").verbose();
const { configureBusinessConnection } = require("../sqlite");

const PRODUCTS = Object.freeze({
    "MAT-000228": { id: 199, title: "Грунтовка Knauf Тифенгрунд 10 л", image: "/uploads/products/MAT-000228-29976ddfd55865e3.webp", token: "SEED_MAT_000228", content: { brand: "KNAUF", short_description: "Готовая к применению грунтовка глубокого проникновения для укрепления основания, снижения впитываемости и улучшения адгезии. Подходит для внутренних и наружных работ.", full_description: "Что это:\nКНАУФ-Тифенгрунд — готовая к применению быстросохнущая паропроницаемая грунтовка глубокого проникновения на основе полимерной дисперсии.\n\nОбласть применения:\nПредназначена для укрепления поверхности основания, снижения его впитывающей способности и улучшения адгезии последующих отделочных материалов.\n\nПодходящие основания:\nПрименяется по гипсокартонным и гипсоволокнистым листам, гипсовым и цементным штукатуркам, гипсовым пазогребневым плитам, гипсовым и цементным стяжкам. Также применяется для обработки стыков ГКЛ и ГКЛВ со снятой фаской перед заделкой гипсовыми шпаклёвками.\n\nОсновные характеристики:\nРасход — около 0,1 кг/м². Время высыхания — около 3 часов. Цвет — белый.\n\nПрименение:\nГрунтовка поставляется готовой к применению. Подходит для внутренних и наружных работ.\n\nУпаковка:\nВедро 10 кг.\n\nХранение:\nСрок хранения — 12 месяцев в неповреждённой упаковке.", seo_title: "Грунтовка KNAUF Тифенгрунд 10 кг — купить в MatMix", seo_description: "KNAUF Тифенгрунд 10 кг — грунтовка глубокого проникновения для укрепления основания и улучшения адгезии. Расход 0,1 кг/м², высыхание около 3 часов." }, attrs: { brand: ["text", "KNAUF"], product_type: ["text", "Грунтовка глубокого проникновения"], base: ["text", "Полимерная дисперсия"], purpose: ["text", "Укрепление основания, снижение впитываемости и улучшение адгезии"], package_weight: ["number", 10, "л"], shelf_life: ["number", 12, "месяцев"] }, skipped: ["consumption_10mm", "coverage_30kg_10mm", "wall_layer_thickness", "ceiling_layer_thickness", "application_temperature"] },
    "MAT-000232": { id: 203, title: "Грунтовка Ceresit CT-17 PRO, 10л", image: "/uploads/products/MAT-000232-c0328be66f62ee89.webp", token: "SEED_MAT_000232", content: { brand: "Ceresit", short_description: "Универсальная грунтовка глубокого проникновения для укрепления впитывающих оснований, связывания пыли и снижения впитывающей способности. Для внутренних и наружных работ.", full_description: "Что это:\nCeresit CT 17 PRO — универсальная водно-дисперсионная грунтовка глубокого проникновения.\n\nОбласть применения:\nПредназначена для обработки впитывающих оснований перед нанесением штукатурных и напольных смесей, плиточных клеев и других отделочных материалов.\n\nПодходящие основания:\nПрименяется по цементным, известковым и гипсовым штукатуркам, цементным и ангидритным стяжкам, лёгкому и ячеистому бетону, кирпичной кладке, природному камню, гипсокартону, древесностружечным и древесноволокнистым плитам и другим впитывающим основаниям.\n\nОсновные характеристики:\nРасход при однократном нанесении — 0,1–0,2 л/м² в зависимости от впитывающей способности основания. Время высыхания — до 2 часов. Цвет плёнки — светло-жёлтый.\n\nПрименение:\nПеред применением содержимое необходимо тщательно перемешать или взболтать. Грунтовку наносят кистью или валиком. Работы выполняют при температуре воздуха и основания от 0 до +35 °C.\n\nУпаковка:\nПластиковая ёмкость 10 л.\n\nХранение:\nСрок хранения — до 24 месяцев в оригинальной герметичной упаковке при соблюдении условий хранения производителя.", seo_title: "Грунтовка Ceresit CT 17 PRO 10 л — купить в MatMix", seo_description: "Ceresit CT 17 PRO 10 л — грунтовка глубокого проникновения для впитывающих оснований. Расход 0,1–0,2 л/м², высыхание до 2 часов." }, attrs: { brand: ["text", "Ceresit"], product_type: ["text", "Универсальная грунтовка глубокого проникновения"], base: ["text", "Водная дисперсия сополимеров акрилатов"], purpose: ["text", "Укрепление впитывающих оснований, связывание пыли и снижение впитываемости"], application_temperature: ["text", "0…+35 °C"], shelf_life: ["number", 24, "месяцев"] }, skipped: ["package_weight", "consumption_10mm", "coverage_30kg_10mm", "wall_layer_thickness", "ceiling_layer_thickness"] },
    "MAT-000335": { id: 294, title: "Блок D500 100х250х600 мм", image: "/uploads/products/MAT-000335-25153e2285e306f4.webp", token: "SEED_MAT_000335", content: { brand: null, short_description: "Газобетонный блок плотностью D500 размером 100×250×600 мм для устройства внутренних стен и перегородок. Толщина блока — 100 мм.", full_description: "Что это:\nГазобетонный блок D500 размером 100×250×600 мм — лёгкий стеновой материал для устройства внутренних стен и перегородок.\n\nОбласть применения:\nПредназначен преимущественно для возведения ненесущих внутренних стен, межкомнатных перегородок и зонирования помещений.\n\nОсновные характеристики:\nМарка плотности — D500. Размер блока — 100×250×600 мм. Объём одного блока — 0,015 м³. Для кладки 1 м² стены требуется ориентировочно 6,7 блока без учёта швов и подрезки.\n\nПрименение:\nБлоки укладывают на предназначенный для газобетона кладочный состав с соблюдением технологии производителя применяемой системы. Основание первого ряда должно быть ровным и подготовленным.\n\nУпаковка:\nПоставка — поштучно. Параметры поддона и количество блоков на поддоне зависят от производителя и партии.\n\nВажно:\nФактического производителя и дополнительные характеристики конкретной партии необходимо уточнять при подтверждении заказа.", seo_title: "Газобетонный блок D500 100×250×600 мм — купить в MatMix", seo_description: "Газобетонный блок D500 100×250×600 мм для внутренних стен и перегородок. Толщина 100 мм, объём блока 0,015 м³. Заказ в MatMix." }, attrs: { product_type: ["text", "Газобетонный блок"], purpose: ["text", "Внутренние стены и перегородки"] }, skipped: ["brand", "base", "package_weight", "consumption_10mm", "coverage_30kg_10mm", "wall_layer_thickness", "ceiling_layer_thickness", "application_temperature", "shelf_life"] }
});

// Contract correction: Tiefengrund is recorded as 10 kg for the typed attribute.
PRODUCTS["MAT-000228"].attrs.package_weight = ["number", 10, "кг"];

function helpers(db) { return { run(sql, params = []) { return new Promise((resolve, reject) => db.run(sql, params, function done(e) { e ? reject(e) : resolve({ id: this.lastID, changes: this.changes }); })); }, get(sql, params = []) { return new Promise((resolve, reject) => db.get(sql, params, (e, row) => e ? reject(e) : resolve(row))); }, all(sql, params = []) { return new Promise((resolve, reject) => db.all(sql, params, (e, rows) => e ? reject(e) : resolve(rows))); }, close() { return new Promise((resolve, reject) => db.close(e => e ? reject(e) : resolve())); } }; }
async function open(file) { const db = new sqlite3.Database(path.resolve(file)); await configureBusinessConnection(db); return helpers(db); }
function equalValue(row, expected) { const number = expected[0] === "number"; return (number ? Number(row.value_number) : row.value_text) === expected[1] && (row.unit_override || null) === (expected[2] || null); }
async function inspect(database, config) {
    const product = await database.get("SELECT * FROM products WHERE external_id=? AND deleted_at IS NULL", [config.external_id]);
    if (!product) throw new Error(`${config.external_id}: товар не найден или удалён.`);
    if (product.id !== config.id || product.title !== config.title) throw new Error(`${config.external_id}: identity guard не пройден.`);
    const codes = Object.keys(config.attrs).concat(config.skipped); const rows = await database.all(`SELECT id,code,data_type,label FROM product_attribute_definitions WHERE code IN (${codes.map(() => "?").join(",")})`, codes); const counts = new Map(); rows.forEach(row => counts.set(row.code, (counts.get(row.code) || 0) + 1)); for (const code of codes) { if (counts.get(code) !== 1) throw new Error(`Definition ${code} разрешается неоднозначно или отсутствует.`); }
    const definitions = new Map(rows.map(row => [row.code, row])); for (const [code, expected] of Object.entries(config.attrs)) if (definitions.get(code).data_type !== expected[0]) throw new Error(`Definition ${code}: ожидался тип ${expected[0]}, получен ${definitions.get(code).data_type}.`);
    const values = await database.all("SELECT v.*,d.code FROM product_attribute_values v JOIN product_attribute_definitions d ON d.id=v.attribute_definition_id WHERE v.product_id=?", [product.id]); const valueCounts = new Map(); values.forEach(row => valueCounts.set(row.code, (valueCounts.get(row.code) || 0) + 1)); for (const [code, count] of valueCounts) if (count > 1) throw new Error(`Дубликат attribute value для ${code}.`);
    const primary = await database.get("SELECT id,image_url,alt_text FROM product_images WHERE product_id=? AND is_primary=1", [product.id]); return { product, definitions, valueByCode: new Map(values.map(row => [row.code, row])), primary, imageMatches: product.image_url === config.image && primary?.image_url === config.image };
}
function semanticChanges(state, config) { const fields = Object.keys(config.content).filter(field => field !== "brand" || config.content.brand !== null).filter(field => (state.product[field] ?? null) !== config.content[field]); const attrs = Object.entries(config.attrs).map(([code, expected]) => ({ code, action: !state.valueByCode.has(code) ? "insert" : equalValue(state.valueByCode.get(code), expected) ? "unchanged" : "update" })); const image = state.imageMatches && state.primary.alt_text !== config.alt ? "update alt_text" : state.imageMatches ? "unchanged" : "warning: image URL mismatch; alt_text unchanged"; return { fields, attributes: attrs, skipped: config.skipped, image }; }
function assertPostcheck(state, config, before) { for (const field of Object.keys(config.content)) if (field !== "brand" || config.content.brand !== null) if ((state.product[field] ?? null) !== config.content[field]) throw new Error(`Postcheck failed: ${field}`); for (const [code, expected] of Object.entries(config.attrs)) if (!state.valueByCode.has(code) || !equalValue(state.valueByCode.get(code), expected)) throw new Error(`Postcheck failed: attribute ${code}`); for (const code of config.skipped) if (before.valueByCode.has(code) && !state.valueByCode.has(code)) throw new Error(`Postcheck failed: skipped attribute removed: ${code}`); if (state.product.image_url !== before.product.image_url || state.primary?.image_url !== before.primary?.image_url) throw new Error("Postcheck failed: image URL changed."); if (config.content.brand === null && state.product.brand !== null) throw new Error("Postcheck failed: brand must remain null."); }
async function apply(database, state, config) { await database.run("BEGIN IMMEDIATE"); try { const repeated = await inspect(database, config); const immutable = ["id", "external_id", "title", "slug", "price", "weight", "unit", "category", "subcategory", "product_group", "image_url", "is_active", "sort_order", "source", "last_imported_at"]; for (const field of immutable) if (repeated.product[field] !== state.product[field]) throw new Error(`Immutable field changed: ${field}`); const now = new Date().toISOString(); const fields = Object.keys(config.content).filter(field => (state.product[field] ?? null) !== config.content[field]); if (fields.length) await database.run("UPDATE products SET brand=?,short_description=?,full_description=?,seo_title=?,seo_description=?,updated_at=? WHERE id=?", [config.content.brand, config.content.short_description, config.content.full_description, config.content.seo_title, config.content.seo_description, now, state.product.id]); for (const [code, expected] of Object.entries(config.attrs)) { const def = state.definitions.get(code); const existing = state.valueByCode.get(code); if (existing && !equalValue(existing, expected)) await database.run("UPDATE product_attribute_values SET value_text=?,value_number=?,value_boolean=NULL,unit_override=?,updated_at=? WHERE id=?", [expected[0] === "text" ? expected[1] : null, expected[0] === "number" ? expected[1] : null, expected[2] || null, now, existing.id]); else if (!existing) await database.run("INSERT INTO product_attribute_values(product_id,attribute_definition_id,value_text,value_number,unit_override,sort_order,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?)", [state.product.id, def.id, expected[0] === "text" ? expected[1] : null, expected[0] === "number" ? expected[1] : null, expected[2] || null, def.sort_order || 0, now, now]); } if (state.imageMatches && state.primary.alt_text !== config.alt) await database.run("UPDATE product_images SET alt_text=?,updated_at=? WHERE id=? AND image_url=? AND is_primary=1", [config.alt, now, state.primary.id, config.image]); const after = await inspect(database, config); if (after.product.image_url !== state.product.image_url || after.primary?.image_url !== state.primary?.image_url) throw new Error("Image preservation guard failed."); await database.run("COMMIT"); return after; } catch (error) { await database.run("ROLLBACK").catch(() => {}); throw error; } }
async function apply(database, state, config) {
    await database.run("BEGIN IMMEDIATE");
    try {
        const baseline = await inspect(database, config);
        const immutable = ["id", "external_id", "title", "slug", "price", "weight", "unit", "category", "subcategory", "product_group", "image_url", "is_active", "sort_order", "source", "last_imported_at"];
        const immutableSnapshot = Object.fromEntries(immutable.map(field => [field, baseline.product[field]]));
        const skippedBaseline = new Map(config.skipped.map(code => [code, baseline.valueByCode.get(code) ? { ...baseline.valueByCode.get(code) } : null]));
        const now = new Date().toISOString();
        const fields = Object.keys(config.content).filter(field => field !== "brand" || config.content.brand !== null).filter(field => (baseline.product[field] ?? null) !== config.content[field]);
        if (fields.length) await database.run("UPDATE products SET brand=?,short_description=?,full_description=?,seo_title=?,seo_description=?,updated_at=? WHERE id=?", [config.content.brand === null ? baseline.product.brand : config.content.brand, config.content.short_description, config.content.full_description, config.content.seo_title, config.content.seo_description, now, baseline.product.id]);
        for (const [code, expected] of Object.entries(config.attrs)) {
            const definition = baseline.definitions.get(code); const existing = baseline.valueByCode.get(code);
            if (existing && !equalValue(existing, expected)) await database.run("UPDATE product_attribute_values SET value_text=?,value_number=?,value_boolean=NULL,unit_override=?,updated_at=? WHERE id=?", [expected[0] === "text" ? expected[1] : null, expected[0] === "number" ? expected[1] : null, expected[2] || null, now, existing.id]);
            else if (!existing) await database.run("INSERT INTO product_attribute_values(product_id,attribute_definition_id,value_text,value_number,unit_override,sort_order,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?)", [baseline.product.id, definition.id, expected[0] === "text" ? expected[1] : null, expected[0] === "number" ? expected[1] : null, expected[2] || null, definition.sort_order || 0, now, now]);
        }
        if (baseline.imageMatches && baseline.primary.alt_text !== config.alt) await database.run("UPDATE product_images SET alt_text=?,updated_at=? WHERE id=? AND image_url=? AND is_primary=1", [config.alt, now, baseline.primary.id, config.image]);
        const after = await inspect(database, config);
        for (const field of Object.keys(config.content)) if (field !== "brand" || config.content.brand !== null) if ((after.product[field] ?? null) !== config.content[field]) throw new Error(`Postcheck failed: content ${field}`);
        if (config.content.brand === null && after.product.brand !== null) throw new Error("Postcheck failed: brand must remain null.");
        for (const [code, expected] of Object.entries(config.attrs)) if (!after.valueByCode.has(code) || !equalValue(after.valueByCode.get(code), expected)) throw new Error(`Postcheck failed: attribute ${code}`);
        for (const code of config.skipped) { const before = skippedBaseline.get(code); const current = after.valueByCode.get(code) || null; if (JSON.stringify(before) !== JSON.stringify(current)) throw new Error(`Postcheck failed: skipped attribute ${code} changed.`); }
        for (const field of immutable) if (after.product[field] !== immutableSnapshot[field]) throw new Error(`Postcheck failed: immutable ${field}`);
        if (after.product.image_url !== immutableSnapshot.image_url || after.primary?.image_url !== baseline.primary?.image_url) throw new Error("Postcheck failed: image URL changed.");
        if (baseline.imageMatches && after.primary?.alt_text !== config.alt) throw new Error("Postcheck failed: image alt.");
        if (!baseline.imageMatches && after.primary?.alt_text !== baseline.primary?.alt_text) throw new Error("Postcheck failed: mismatched image alt changed.");
        await database.run("COMMIT");
        return after;
    } catch (error) { await database.run("ROLLBACK").catch(() => {}); throw error; }
}

async function runSeed({ database, externalId, apply: shouldApply = false, confirm = "" } = {}) { const source = PRODUCTS[externalId]; if (!source) throw new Error(`Неизвестный SKU: ${externalId}`); const altBySku = { "MAT-000228": "Грунтовка KNAUF Тифенгрунд 10 кг", "MAT-000232": "Грунтовка Ceresit CT 17 PRO 10 л", "MAT-000335": "Газобетонный блок D500 100×250×600 мм" }; const config = { ...source, external_id: externalId, alt: source.alt || altBySku[externalId] || source.title }; const state = await inspect(database, config); const changes = semanticChanges(state, config); if (!shouldApply) return { changed: false, state, changes }; if (confirm !== config.token) throw new Error(`Для apply требуется --confirm ${config.token}.`); const hasChanges = changes.fields.length > 0 || changes.attributes.some(item => item.action !== "unchanged") || changes.image === "update alt_text"; const after = await apply(database, state, config); return { changed: hasChanges, state: after, changes: semanticChanges(after, config) }; }
async function main() { const args = process.argv.slice(2); const externalId = args.find(arg => arg.startsWith("--sku="))?.slice(6); const file = args.find(arg => arg.startsWith("--db="))?.slice(5) || process.env.MATMIX_DB_PATH || path.join(__dirname, "..", "database", "matmix.db"); const confirmIndex = args.indexOf("--confirm"); const database = await open(file); try { console.log(JSON.stringify({ database: path.resolve(file), ...(await runSeed({ database, externalId, apply: args.includes("--apply"), confirm: confirmIndex >= 0 ? args[confirmIndex + 1] : "" })) }, null, 2)); } finally { await database.close(); } }
module.exports = { PRODUCTS, inspect, runSeed };
if (require.main === module) main().catch(error => { console.error(`SEED ABORTED: ${error.message}`); process.exitCode = 1; });
