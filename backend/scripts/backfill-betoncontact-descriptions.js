"use strict";
const fs = require("fs"); const path = require("path"); const sqlite3 = require("sqlite3").verbose(); const DATA = require("./data/betoncontact-descriptions");
const CONFIRM = "BACKFILL_BETONCONTACT_DESCRIPTIONS"; const ALL_MATS = Object.freeze(DATA.TARGET_MATS); const DESCRIPTION_MUTABLE_FIELDS_EXACTLY = Object.freeze(["full_description"]);
const IMMUTABLE_FIELDS = Object.freeze(["title", "slug", "brand", "weight", "unit", "price", "category", "subcategory", "short_description", "description", "seo_title", "seo_description", "stock_status", "image", "image_url"]);
const PUBLIC_META_MARKERS = Object.freeze(["для этой карточки", "в источнике", "официальный источник", "локальный", "needs_source", "research", "review", "schema_blocked"]);
const normalizeStructure = value => String(value ?? "").normalize("NFKC").replace(/\s+/gu, " ").trim().toLocaleLowerCase("ru-RU"); const nonempty = value => value !== null && value !== undefined && String(value).trim() !== "";
function normalizeOnly(only) { if (typeof only === "string") return only.split(",").map(value => value.trim()); if (Array.isArray(only)) return only.map(value => String(value).trim()); throw new Error("Exact description batch is required"); }
function assertExactBatch(only) { const selected = normalizeOnly(only); if (selected.length !== ALL_MATS.length || new Set(selected).size !== ALL_MATS.length || selected.some((value, index) => value !== ALL_MATS[index])) throw new Error(`Exact description batch required: ${ALL_MATS.join(",")}`); return selected; }
function openDatabase(file, writable = false) { if (!file || file === ":memory:") throw new Error("Explicit --db path is required"); const resolved = path.resolve(file); const mode = writable ? sqlite3.OPEN_READWRITE : sqlite3.OPEN_READONLY; return new Promise((resolve, reject) => { const raw = new sqlite3.Database(resolved, mode, error => { if (error) return reject(error); const db = { run(sql, params = []) { return new Promise((res, rej) => raw.run(sql, params, function done(e) { e ? rej(e) : res({ id: this.lastID, changes: this.changes }); })); }, get(sql, params = []) { return new Promise((res, rej) => raw.get(sql, params, (e, row) => e ? rej(e) : res(row))); }, all(sql, params = []) { return new Promise((res, rej) => raw.all(sql, params, (e, rows) => e ? rej(e) : res(rows))); }, close() { return new Promise((res, rej) => raw.close(e => e ? rej(e) : res())); } }; db.run("PRAGMA foreign_keys=ON").then(() => writable ? db : db.run("PRAGMA query_only=ON").then(() => db)).then(resolve).catch(async e => { await db.close(); reject(e); }); }); }); }
function parseArgs(args) { const out = { apply: false }; const seen = new Set(); for (let i = 0; i < args.length; i += 1) { const [key, ...tail] = args[i].split("="); if (seen.has(key)) throw new Error(`Duplicate option: ${key}`); seen.add(key); if (key === "--apply" || key === "--dry-run") { if (tail.length) throw new Error(`Unexpected value: ${key}`); out.apply = key === "--apply"; continue; } if (!["--db", "--only", "--confirm", "--backup-dir", "--review"].includes(key)) throw new Error(`Unknown option: ${key}`); const value = tail.length ? tail.join("=") : args[++i]; if (!value || value.startsWith("--")) throw new Error(`Value required: ${key}`); out[key.slice(2)] = value; } if (seen.has("--apply") && seen.has("--dry-run")) throw new Error("Choose either --dry-run or --apply"); if (!out.db || out.db === ":memory:") throw new Error("Explicit --db path is required"); if (out.only === undefined) throw new Error("Explicit --only is required"); out.only = assertExactBatch(out.only); if (seen.has("--confirm") && !out.apply) throw new Error("--confirm requires --apply"); if (out.apply && out.confirm !== CONFIRM) throw new Error(`Apply requires --confirm ${CONFIRM}`); return out; }
function snapshot(product) { return Object.fromEntries(IMMUTABLE_FIELDS.map(field => [field, product[field] ?? null])); }
function sameSnapshot(before, after) { return IMMUTABLE_FIELDS.every(field => (before[field] ?? null) === (after[field] ?? null)); }
function validatePublicDescription(value) { const lower = String(value || "").toLocaleLowerCase("ru-RU"); const found = PUBLIC_META_MARKERS.filter(marker => lower.includes(marker)); if (found.length) throw new Error(`Public description contains review/meta language: ${found.join(", ")}`); }
function validateSourceKeys(data) { const known = new Set(Object.keys(data.SOURCES || {})); const bad = data.PRODUCTS.flatMap(item => (item.sourceKeys || []).filter(key => !known.has(key)).map(key => `${item.externalId}:${key}`)); if (bad.length) throw new Error(`Dangling source registry keys: ${bad.join(", ")}`); }
function guard(product, config) { if (!product || Number(product.is_active) !== 1 || product.deleted_at) throw new Error("Product missing, inactive, or deleted"); if (product.title !== config.expectedTitle) throw new Error(`Exact title guard failed: expected «${config.expectedTitle}», got «${product.title}»`); if (normalizeStructure(product.category) !== normalizeStructure(config.expectedCategory) || normalizeStructure(product.subcategory) !== normalizeStructure(config.expectedSubcategory)) throw new Error(`Category/subcategory guard failed: got ${product.category} / ${product.subcategory}`); }
async function inspectBatch(db, { only, data = DATA } = {}) { validateSourceKeys(data); const selected = assertExactBatch(only); const rows = []; for (const externalId of selected) { const config = data.PRODUCTS.find(item => item.externalId === externalId); try { validatePublicDescription(config.proposedDescription); const product = await db.get("SELECT * FROM products WHERE external_id=?", [externalId]); guard(product, config); const current = product.full_description ?? null; const status = !nonempty(current) ? "WILL_ADD" : current === config.proposedDescription ? "EXISTING_OK" : "CONTENT_CONFLICT"; rows.push({ externalId, productId: product.id, title: product.title, currentDescription: current, proposedDescription: config.proposedDescription, status, sourceKeys: config.sourceKeys, immutableSnapshot: snapshot(product), characterCount: Array.from(config.proposedDescription).length }); } catch (error) { rows.push({ externalId, title: config.expectedTitle, status: "ERROR", error: error.message, sourceKeys: config.sourceKeys }); } } return { mode: "dry-run", rows, sources: data.SOURCES, writableSurface: { products: DESCRIPTION_MUTABLE_FIELDS_EXACTLY, forbidden: IMMUTABLE_FIELDS.concat(["all product_attribute_values", "all product_images"]) }, summary: { total: rows.length, willAdd: rows.filter(r => r.status === "WILL_ADD").length, existingOk: rows.filter(r => r.status === "EXISTING_OK").length, contentConflict: rows.filter(r => r.status === "CONTENT_CONFLICT").length, errors: rows.filter(r => r.status === "ERROR").length, excludedAllowlist: 0 } }; }
async function backupDatabase(dbPath, backupDir) { const dir = path.resolve(backupDir || path.join(path.dirname(dbPath), "backups")); fs.mkdirSync(dir, { recursive: true }); const target = path.join(dir, `matmix-before-betoncontact-descriptions-${Date.now()}.db`); fs.copyFileSync(path.resolve(dbPath), target); return target; }
async function applyBatch(db, dbPath, options, data = DATA) { if (options.confirm !== CONFIRM) throw new Error(`Apply requires --confirm ${CONFIRM}`); const preflight = await inspectBatch(db, { only: options.only, data }); if (preflight.summary.errors || preflight.summary.contentConflict) throw new Error("Description apply blocked by guard/conflict"); const targets = preflight.rows.filter(row => row.status === "WILL_ADD"); if (!targets.length) return { ...preflight, mode: "apply", writes: 0, backup: null }; const backup = await backupDatabase(dbPath, options.backupDir); await db.run("BEGIN IMMEDIATE"); let writes = 0; try { const now = new Date().toISOString(); for (const row of targets) { const result = await db.run("UPDATE products SET full_description=?,updated_at=? WHERE id=? AND external_id=? AND (full_description IS NULL OR TRIM(full_description)='')", [row.proposedDescription, now, row.productId, row.externalId]); if (result.changes !== 1) throw new Error(`Write guard failed: ${row.externalId}`); writes += 1; } for (const row of targets) { const after = await db.get("SELECT * FROM products WHERE id=? AND external_id=?", [row.productId, row.externalId]); if (!after || after.full_description !== row.proposedDescription || !sameSnapshot(row.immutableSnapshot, after)) throw new Error(`Postcheck failed: ${row.externalId}`); } await db.run("COMMIT"); return { ...preflight, mode: "apply", writes, backup }; } catch (error) { await db.run("ROLLBACK").catch(() => {}); throw error; } }
function md(value) { return String(value ?? "—").replace(/\|/g, "\\|").replace(/\n/g, "<br>"); }
function renderReview(report) { const lines = ["# Бетонконтакт — descriptions review", "", "Изменяется только `products.full_description`; title, SEO, attributes, images, price, stock и другие product fields не изменяются.", "", `DESCRIPTION_MUTABLE_FIELDS_EXACTLY = ["full_description"]`, `CONFIRM = ${CONFIRM}`, "", "## Summary", "", "```json", JSON.stringify(report.summary, null, 2), "```", "", "| MAT | Status | Characters | Source keys |", "|---|---|---:|---|"]; for (const row of report.rows) lines.push(`| ${row.externalId} | ${row.status} | ${row.characterCount || 0} | ${md((row.sourceKeys || []).join(", "))} |`); lines.push("", "## Proposed descriptions", ""); for (const row of report.rows) lines.push(`### ${row.externalId}`, "", `- Title: ${md(row.title)}`, `- Status: **${row.status}**`, `- Source keys: ${md((row.sourceKeys || []).join(", "))}`, "", row.proposedDescription || "—", ""); lines.push("## Source registry", ""); for (const [key, source] of Object.entries(report.sources || {})) lines.push(`- **${key}**: [${source.title}](${source.url}) — ${source.owner}`); return lines.join("\n") + "\n"; }
async function main() { const options = parseArgs(process.argv.slice(2)); const db = await openDatabase(options.db, options.apply); try { const report = options.apply ? await applyBatch(db, options.db, options) : await inspectBatch(db, options); if (options.review) { const base = path.resolve(options.review); fs.mkdirSync(path.dirname(base), { recursive: true }); fs.writeFileSync(`${base}.json`, JSON.stringify(report, null, 2) + "\n"); fs.writeFileSync(`${base}.md`, renderReview(report)); } for (const row of report.rows) console.log(JSON.stringify(row)); console.log(JSON.stringify({ mode: report.mode, summary: report.summary })); if (report.summary.errors || report.summary.contentConflict) process.exitCode = 1; } finally { await db.close(); } }
module.exports = { ALL_MATS, CONFIRM, DATA, DESCRIPTION_MUTABLE_FIELDS_EXACTLY, IMMUTABLE_FIELDS, applyBatch, assertExactBatch, inspectBatch, normalizeOnly, normalizeStructure, openDatabase, parseArgs, renderReview };
if (require.main === module) main().catch(error => { console.error(`BETONCONTACT DESCRIPTION BACKFILL ABORTED: ${error.message}`); process.exitCode = 1; });
