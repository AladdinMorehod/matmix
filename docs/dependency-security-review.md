# MatMix dependency security review

Review date: 2026-07-15. Source: `npm audit --omit=dev --json`, `npm ls`, package source inspection. No `npm audit fix`, override or package upgrade was applied.

The production audit reports 9 affected package entries: 5 high, 2 moderate, 2 low, 0 critical. Severity below is the registry severity and has not been reduced. “Reachability” only describes the observed MatMix call path; it is not a severity override.

| Package / advisory | Severity | Chain and scope | MatMix reachability / attack surface | Patch and upgrade risk | Recommendation |
| --- | --- | --- | --- | --- | --- |
| `sqlite3@5.1.7` | High | Direct production dependency; affected through bundled `node-gyp`/`tar` install tooling | SQLite runtime queries do not call the vulnerable archive tooling. Exposure is primarily dependency installation/native rebuild, not an HTTP route. | Audit proposes `sqlite3@6.0.1`, a major update requiring API/native-build and full DB regression testing. | Formal temporary risk acceptance may be considered only for an immutable `npm ci` build from the committed lockfile and trusted registry. Plan a separate `sqlite3@6` upgrade before broad public launch. |
| `node-gyp@8.4.1` | High | `sqlite3 -> node-gyp` | Build/install tooling; not called by MatMix request handlers. Risk applies when compiling/installing dependencies. | Fixed chain requires the `sqlite3@6` major path. A blind override can break native builds. | Same explicit acceptance as `sqlite3`; build in an isolated non-runtime account and deploy immutable artifacts. Investigate upgrade separately. |
| `tar@6.2.1` (`GHSA-34x7-hfp2-rc4v`, `GHSA-8qq5-rm4j-mr97`, `GHSA-83g3-92jg-28cx`, `GHSA-qffp-2rhf-9h96`, `GHSA-9ppj-qmqm-q256`, `GHSA-r6q2-hw4h-h46w`, `GHSA-vmf3-w455-68vh`) | High | `sqlite3 -> tar`; also `sqlite3 -> node-gyp -> tar/cacache` | MatMix application code does not extract tar files. Surface is package/native-binary installation and any compromised/untrusted archive input during build. | Patched `tar` versions exist, but the lock is constrained through old build dependencies. An override across several majors is not proven compatible. | Do not accept untrusted build archives. Prefer upgrading the parent chain; do not add an untested override immediately before launch. Requires written acceptance if launch precedes upgrade. |
| `make-fetch-happen@9.1.0` | High | `sqlite3 -> node-gyp -> make-fetch-happen` | Build-time network/cache path; not used by runtime HTTP code. | Parent upgrade required; isolated override can destabilize `node-gyp@8`. | Temporary acceptance only with trusted registry, lockfile, isolated CI and no install scripts on the production host where avoidable. |
| `cacache@15.3.0` | High | `sqlite3 -> node-gyp -> make-fetch-happen -> cacache -> tar` | npm/node-gyp cache handling during build; no observed runtime call path. | Parent/major upgrade required. | Same build-chain acceptance; remediate with the parent upgrade rather than a point override. |
| `http-proxy-agent@4.0.1` | Low | `sqlite3 -> node-gyp -> make-fetch-happen -> http-proxy-agent` | Build-time proxy support; MatMix runtime proxying uses Nginx/Express, not this package. | Fixed chain requires parent upgrades. | Accept temporarily with the build-chain controls; review when upgrading `sqlite3`. |
| `@tootallnate/once@1.1.2` (`GHSA-vpq2-c234-7xj6`) | Low | `... -> http-proxy-agent -> @tootallnate/once` | Build tooling only in the observed dependency tree. | Parent upgrade required. | Temporary acceptance is reasonable with documented build controls; not a standalone launch blocker. |
| `exceljs@4.4.0` | Moderate | Direct production dependency; affected through `uuid@8.3.2` | Excel import/export is runtime and processes uploaded workbooks, so the chain is security relevant. The advisory is in UUID v3/v5/v6 buffer handling; inspected ExcelJS code uses `uuid.v4()` for conditional-formatting IDs. The known vulnerable API is therefore not observed in this path. | Audit suggests a downgrade/major-changing resolution and is not safe to apply automatically. | Documented temporary acceptance is possible based on the v4-only observed call. Keep workbook size/type/formula protections and schedule an ExcelJS/UUID compatibility test. |
| `uuid@8.3.2` (`GHSA-w5hq-g745-h8pq`) | Moderate | `exceljs -> uuid` | Vulnerability concerns caller-supplied buffers in v3/v5/v6. MatMix does not import UUID directly; ExcelJS source references `v4()` only. This is evidence of non-reachability for the known path, not a severity reduction. | Patched `uuid>=11.1.1`; forcing a major override outside ExcelJS's declared range is risky. | Temporarily accept only with the recorded source evidence; retest import/export if ExcelJS or UUID changes. |

## Launch decision

There is no critical advisory. The low entries and the UUID path do not independently block a restricted soft launch. The five high build-chain entries require a named security/operations owner to sign a time-bounded risk acceptance, with locked `npm ci`, trusted registry, isolated build, immutable deployment and a scheduled `sqlite3@6` compatibility project. Without that written acceptance, the dependency gate remains blocking. Re-run both `npm audit --json` and `npm audit --omit=dev --json` immediately before release because registry data changes.
